# Project Ideas & Future Roadmap

## Data Storage & Persistence
- **[COMPLETED] IndexedDB for Streaming History**: Implemented IndexedDB to store the user's parsed streaming history data client-side. Allows the app to persist massive datasets (like 300MB+ JSON archives) reliably.
